spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ShowTime
    username: postgres
    password: 4421

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  error:
    include-message: always
    include-stacktrace: always

jwt:
  secret: showtime
  exp: 50000